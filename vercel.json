{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm exec turbo build --filter=@currentstate/web",
  "outputDirectory": "apps/web/.next"
}
